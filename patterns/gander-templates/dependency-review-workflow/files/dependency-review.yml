name: Dependency Review

on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

jobs:
  # Detect dependency changes
  changes:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
    outputs:
      dependencies: ${{ steps.filter.outputs.dependencies }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: dorny/paths-filter@de90cc6fb38fc0963ad72b210f1f284cd68cea36 # v3.0.2
        id: filter
        with:
          filters: |
            dependencies:
              - 'package.json'
              - 'package-lock.json'

  # Review dependency changes
  dependency-review:
    needs: changes
    # Requires Dependency graph to be enabled
    # To enable on your repository:
    # 1. Go to Settings → Code security and analysis
    # 2. Enable "Dependency graph"
    # 3. Enable "Dependabot alerts" (recommended)
    if: needs.changes.outputs.dependencies == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Dependency Review
        uses: actions/dependency-review-action@3c4e3dcb1aa7874d2c16be7d79418e9b7efd6261 # v4.8.2
        with:
          # Fail on critical and high vulnerabilities
          fail-on-severity: moderate
          # Warn on low OpenSSF Scorecard
          warn-on-openssf-scorecard-level: 3
          # Comment on PR with results
          comment-summary-in-pr: always
          # Show OpenSSF Scorecard metrics
          show-openssf-scorecard: true
          # Deny licenses
          deny-licenses: GPL-2.0, GPL-3.0
          # Allow licenses (optional - comment out to allow all)
          # allow-licenses: MIT, Apache-2.0, BSD-3-Clause

  # Summary
  review-summary:
    needs: dependency-review
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Dependency Review Summary
        run: |
          {
            echo "## 🔍 Dependency Review Results"
            echo ""
            echo "- **Status:** ${{ needs.dependency-review.result }}"
            echo ""
            if [ "${{ needs.dependency-review.result }}" == "success" ]; then
              echo "✅ No security vulnerabilities or license issues found."
            else
              echo "❌ Security vulnerabilities or license issues detected. Check the dependency review job for details."
            fi
          } >> "$GITHUB_STEP_SUMMARY"
